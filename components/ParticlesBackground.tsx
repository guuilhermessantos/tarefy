'use client';

export function ParticlesBackground() {
  // Simplified background - particles removed for build compatibility
  return (
    <div className="absolute inset-0 z-0 bg-background" />
  );
}

